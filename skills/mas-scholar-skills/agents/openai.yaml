interface:
  display_name: "MAS Scholar Skills"
  short_description: "Route required MAS paper or MAG grant capabilities"
  default_prompt: "Use $mas-scholar-skills for the required MAS paper or MAG grant capability set, preserving fail-closed dependency checks and refs-only owner boundaries."

policy:
  allow_implicit_invocation: true
